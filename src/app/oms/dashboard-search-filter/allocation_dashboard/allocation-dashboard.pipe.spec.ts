import { AllocationDashboardPipe } from './allocation-dashboard.pipe';

describe('AllocationDashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new AllocationDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
