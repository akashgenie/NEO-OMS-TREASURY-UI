import { BlockDashboardSearchPipe } from './block-dashboard-search.pipe';

describe('BlockDashboardSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BlockDashboardSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
