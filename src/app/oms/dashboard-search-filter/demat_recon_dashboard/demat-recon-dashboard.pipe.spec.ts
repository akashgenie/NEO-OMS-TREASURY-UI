import { DematReconDashboardPipe } from './demat-recon-dashboard.pipe';

describe('DematReconDashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new DematReconDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
