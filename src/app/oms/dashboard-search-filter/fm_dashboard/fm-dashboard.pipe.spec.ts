import { FmDashboardPipe } from './fm-dashboard.pipe';

describe('FmDashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new FmDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
