import { MonitoringDashboardPipe } from './monitoring-dashboard.pipe';

describe('MonitoringDashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new MonitoringDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
