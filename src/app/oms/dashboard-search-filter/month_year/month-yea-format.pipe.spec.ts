import { MonthYeaFormatPipe } from './month-yea-format.pipe';

describe('MonthYeaFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthYeaFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
