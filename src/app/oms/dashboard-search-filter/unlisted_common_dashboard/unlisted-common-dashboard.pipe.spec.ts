import { UnlistedCommonDashboardPipe } from './unlisted-common-dashboard.pipe';

describe('UnlistedCommonDashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new UnlistedCommonDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
