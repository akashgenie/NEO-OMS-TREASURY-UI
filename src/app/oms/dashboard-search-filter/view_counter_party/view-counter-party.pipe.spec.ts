import { ViewCounterPartyPipe } from './view-counter-party.pipe';

describe('ViewCounterPartyPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewCounterPartyPipe();
    expect(pipe).toBeTruthy();
  });
});
