import { WsaFileGenPipe } from './wsa-file-gen.pipe';

describe('WsaFileGenPipe', () => {
  it('create an instance', () => {
    const pipe = new WsaFileGenPipe();
    expect(pipe).toBeTruthy();
  });
});
