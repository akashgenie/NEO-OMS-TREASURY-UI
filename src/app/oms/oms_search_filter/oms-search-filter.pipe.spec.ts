import { OmsSearchFilterPipe } from './oms-search-filter.pipe';

describe('OmsSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OmsSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
