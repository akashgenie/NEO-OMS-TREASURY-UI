import { PMSIndianCurrencyPipe } from './pms-currency.pipe';

describe('PMSIndianCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new PMSIndianCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
