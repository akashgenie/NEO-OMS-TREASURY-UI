import { CommaSeperatedPipe } from './comma-seperated.pipe';

describe('CommaSeperatedPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSeperatedPipe();
    expect(pipe).toBeTruthy();
  });
});
